#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _puts2 - prints every other character
 * str : string
 *
 */

void _puts2(char *str)
{
	int len = strlen(str)
for (str = 0; str < len; ++str)
	{
		putchar(*str);
	}
}
